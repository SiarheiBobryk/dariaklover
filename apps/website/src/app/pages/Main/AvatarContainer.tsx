import { styled } from '@mui/material/styles';

const AvatarContainer = styled('div')(function styleAvatarContainer() {
  return {
    display: 'flex',
    justifyContent: 'center',
  };
});

export default AvatarContainer;
