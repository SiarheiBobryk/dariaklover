export interface AboutMetaData {
  title: string;
  heading: string;
  path: string;
}

const aboutMetaData: AboutMetaData = {
  title: 'Обо мне',
  heading: 'Обо мне',
  path: '/about',
};

export default aboutMetaData;
